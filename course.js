class Course {
    constructor(location, preferences, budget) {
        this.location = location;
        this.preferences = preferences;
        this.budget = budget;
    }
    // 필요한 메서드 추가
}

module.exports = Course;
